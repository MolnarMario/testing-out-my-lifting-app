# Ironlog — Powerlifting Tracker

A self-contained powerlifting tracker web app. The entire application lives in a single file, [`ironlog.html`](ironlog.html) — just open it in a browser to run it.

## Usage

Open `ironlog.html` in any modern web browser. No build step or server required.
